# These are more detailed classes, which are optionally used for some calculations that require them.
# Most calculations only require high level statistics, so these properties are not always modeled
